; Do not change filenames or add or remove FILEI/FILEO statements using an editor. Use Cube/Application Manager.
RUN PGM=NETWORK MSG='Check for "zero"'
FILEO PRINTO[1] = "Output.csv"
FILEI LINKI[1] = "I-85 EL Network 6.net"

; Print Header 
_Count=_Count+1

IF (_Count=1)
  PRINT CSV=T LIST='        A,         B,    NUM_LANES,    CAPACITY,    DISTANCE,    POSTSPD,    FTC2' PRINTO=1
  
ENDIF

; Number of Lanes
IF (li.1.NUM_LANES=0)

  PRINT CSV=T LIST=A(10),B(10),LI.1.NUM_LANES(10), CAPACITY(10), DISTANCE(10.2), POSTSPD(10.2), FTC2(10), PRINTO=1 

ENDIF

; Capacity 
IF (li.1.CAPACITY=0)

  PRINT CSV=T LIST=A(10),B(10),LI.1.NUM_LANES(10), CAPACITY(10), DISTANCE(10.2), POSTSPD(10.2), FTC2(10), PRINTO=1 

ENDIF

; Distance 
IF (li.1.DISTANCE=0)

  PRINT CSV=T LIST=A(10),B(10),LI.1.NUM_LANES(10), CAPACITY(10), DISTANCE(10.2), POSTSPD(10.2), FTC2(10), PRINTO=1 

ENDIF  
  
; POSTSPD 
IF (li.1.POSTSPD=0)

  PRINT CSV=T LIST=A(10),B(10),LI.1.NUM_LANES(10), CAPACITY(10), DISTANCE(10.2), POSTSPD(10.2), FTC2(10), PRINTO=1 

ENDIF

; FTC2
IF (li.1.FTC2=0)

  PRINT CSV=T LIST=A(10),B(10),LI.1.NUM_LANES(10), CAPACITY(10), DISTANCE(10.2), POSTSPD(10.2), FTC2(10), PRINTO=1 

ENDIF


ENDRUN
